{
  "name": "actionslow-action",
  "version": "0.8.0",
  "description": "Action to trigger Actionslaw workflows",
  "main": "dist/main.js",
  "author": "Ric Wood <ric@grislyeye.com>",
  "scripts": {
    "build": "tsc && npm run pack",
    "start": "node dist/main.js",
    "pack": "esbuild lib/main.js --outfile=dist/main.js  --bundle --platform=node --target=node20"
  },
  "devDependencies": {
    "@actions/core": "^1.10.1",
    "@tsconfig/node20": "^20.1.2",
    "@types/node": "^20.10.5",
    "esbuild": "^0.19.10",
    "resolve-cwd": "^3.0.0"
  },
  "dependencies": {
    "rss-parser": "^3.13.0"
  }
}
